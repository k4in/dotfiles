---
description: Commit and push with a fitting message, no description
---

Only work in the git repo of the current working directory (do not touch parent/sibling/other repos). Check which changes are currently open there (they can differ from this session history), then commit and push those changes with a fitting message (english) into the current branch, no description.
